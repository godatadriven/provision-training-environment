- name: Install needed packages
  apt: 
    name: "{{ item }}"
    state: latest
  with_items:
    - bzip2
    - zip
    - unzip
    # - python3-dev
    - python3-pip
    - build-essential
    - gcc
    - libfreetype6-dev
    - blt-dev
    - pkg-config
    - netcat
    # - llvm-3.8-dev
    # - libssl1.0.0
    - sudo

